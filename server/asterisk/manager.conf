; AMI — how the switchboard app reloads pjsip and reads registrations.
; Loopback only; CHANGE the secret (and mirror it in AMI_PASS for the app).

[general]
enabled = yes
bindaddr = 127.0.0.1
port = 5038

[switchboard]
secret = changeme-ami
read = command
write = command
